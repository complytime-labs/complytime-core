services:
  nats:
    image: nats:2.14-alpine
    command: ["-c", "/etc/nats/nats-server.conf"]
    volumes:
      - ./nats-server.conf:/etc/nats/nats-server.conf:ro
      - nats-data:/data/jetstream
    ports:
      - "4222:4222"
      - "8222:8222"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8222/varz"]
      interval: 10s
      timeout: 5s
      retries: 3

  testjwks:
    build:
      context: ../..
      dockerfile: Dockerfile.testjwks
    volumes:
      - service-tokens:/tokens
    ports:
      - "8888:8888"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8888/healthz"]
      interval: 5s
      timeout: 3s
      retries: 5

  locker:
    build:
      context: ../..
      dockerfile: Dockerfile.locker
    environment:
      - LOCKER_DATA_PATH=/data/ledgers
      - LOCKER_LISTEN_ADDR=:8081
      - NATS_URL=nats://nats:4222
      - JWT_ISSUERS=http://testjwks:8888
      - JWT_AUDIENCE=complytime-locker
    volumes:
      - locker-data:/data/ledgers
    ports:
      - "8081:8081"
    depends_on:
      nats:
        condition: service_healthy
      testjwks:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8081/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3

  gateway:
    build:
      context: ../..
      dockerfile: Dockerfile.gateway
    environment:
      - NATS_URL=nats://nats:4222
      - JWT_ISSUERS=http://testjwks:8888
      - JWT_AUDIENCE=complytime-gateway
      - GATEWAY_LISTEN_ADDR=:8080
    ports:
      - "8080:8080"
    depends_on:
      nats:
        condition: service_healthy
      locker:
        condition: service_healthy
      testjwks:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  nats-data:
  locker-data:
  service-tokens:
